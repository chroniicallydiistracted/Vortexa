{
  "name": "alerts-lambda",
  "version": "0.1.0",
  "type": "module",
  "main": "dist/index.js",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "clean": "rm -rf dist",
    "seed": "tsx src/seed.ts",
    "seed:built": "node dist/index.js"
  },
  "dependencies": {
    "@aws-sdk/client-dynamodb": "^3.588.0",
    "@aws-sdk/lib-dynamodb": "^3.588.0"
  },
  "devDependencies": {
    "typescript": "^5.4.0",
    "tsx": "^4.19.1"
  }
}
